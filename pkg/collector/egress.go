package collector

// Egress accounting and egress-volume signal emission were removed in the
// minimal branch. The collector no longer emits SIGNAL_EGRESS_VOLUME, and the
// analyzer no longer consumes sustained-download volume signals.
